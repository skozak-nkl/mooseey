let youtubeButtons = document.querySelectorAll(".youtube-play");

youtubeButtons.forEach(function(btn) {
    btn.addEventListener('click', (event) => {
        alert('Not Implemented Yet!');
    })
});